import { DevCatalogue } from "../src/dev/DevCatalogue";

/**
 * The `@gryt/ui-native` catalogue, pushed over the tab bar rather than given a tab. It
 * is a harness for feedback, reached from the "you" sheet as on the desktop.
 */
export default DevCatalogue;

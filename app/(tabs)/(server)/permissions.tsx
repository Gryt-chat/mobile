import { PermissionTemplatesScreen } from "../../../src/permissions/PermissionTemplatesScreen";

/**
 * Channel permission templates, pushed inside the Server tab. `ConnectionsProvider` is
 * mounted in the tabs layout, so a root route threw on mount and typechecked fine.
 */
export default PermissionTemplatesScreen;

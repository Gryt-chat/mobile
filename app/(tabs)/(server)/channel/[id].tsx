import { ChannelScreen } from "../../../../src/shell/ChannelScreen";

/**
 * A text channel, pushed over the tab bar — inside `(tabs)` rather than beside it, so
 * the bar stays visible while you read a channel.
 */
export default ChannelScreen;

import { BanListScreen } from "../../../src/moderation/BanListScreen";

/**
 * The ban list, pushed inside the Server tab. `ConnectionsProvider` is mounted in the
 * tabs layout, so a root route typechecks and throws on mount.
 */
export default BanListScreen;

import { ChannelScopeScreen } from "../../../src/permissions/ChannelScopeScreen";

/**
 * Who can use one channel, pushed inside the Server tab. `ConnectionsProvider` is
 * mounted in the tabs layout, so a root route typechecks and throws on mount.
 */
export default ChannelScopeScreen;

import { BanScreen } from "../../../../src/moderation/BanScreen";

/**
 * The ban form, pushed inside the Server tab. `ConnectionsProvider` is mounted in the
 * tabs layout, so this is the only place `useServerConnection` resolves.
 */
export default BanScreen;

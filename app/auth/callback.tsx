import { AuthCallbackScreen } from "../../src/account/AuthCallbackScreen";

/**
 * `gryt://auth/callback`, the redirect Keycloak sends people back to. Registered on the
 * `gryt-web` client and hardcoded in `authServer.ts`, so it is not ours to rename.
 */
export default AuthCallbackScreen;

name: Discord CI notification

# Reports failed workflow runs to #ci in Discord, and the recovery. Implementation in
# Gryt-chat/.github. List new workflows here — an unlisted one notifies nobody.

on:
  workflow_run:
    workflows:
      - Android
      - CI
      - Expo versions
      - Release Android
      - Release iOS
      - Sync Vikunja task
    types: [completed]

jobs:
  notify:
    uses: Gryt-chat/.github/.github/workflows/discord-ci-notify.yml@main
    secrets: inherit
